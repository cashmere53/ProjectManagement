<!--############################################################
# specificat on ;
# name     = Good Measure ;
# note     = ;
# date     = 2015/12/6 ;
# author   = Nishi ;
# History  = ver1.0.0 ;
# end of specification ;
#############################################################-->

<%= render 'shared/library' %>
<%= render 'shared/incmenu' %>

<div style="#999999">
<div class="container-fluid" style="margin:20px;"><div class="row">
  <ul class="nav nav-tabs">
   <li><a href="/housing_databases/showTables/<%= @inc_account.id %>">物件</a></li>
   <li class="active"><a href="/advertising_databases/showTables/<%= @inc_account.id %>">広告</a></li>
    <li><a href="/store/showStores/<%= @inc_account.id %>">店舗</a></li>
  </ul>
    <div align="right"><%= button_to "広告登録", advertising_databases_registerDatabses_path(@inc_account_id), method: :get, class: "btn btn-success", style: "margin:5px;"%></div>
  <div class="container">
    <table class="table">
      <thead>
      <tr>
        <th>広告情報<%= @mexx %></th>
        <th>イメージ</th>
        <th>契約更新日</th>
      </tr>
      </thead>

      <tbody>
      <% @advertising.each do |info| %>
          <tr>
            <td><%= info.advertise_info %></td>
            <td><%= image_tag(advertising_databases_image_path(info.id), :size => '100x100') %></td>
            <td><%= info.contract_date %></td>
            <td><%= link_to '編集', advertising_databases_editDatabases_path(info.id), class: "btn btn-warning" %> <%= link_to '削除', advertising_databases_destroy_path(info.id),method: 'delete', class: "btn btn-warning" ,"data-confirm" => "本当に削除しますか？"%></td>
          </tr>
      <% end %>
      </tbody>
    </table>
</div></div></div>
